export const statusFilters = Object.freeze({
  all: 'all',
  toDo: 'to-do',
  inProgress: 'in-progress',
  done: 'done',
});

export const priority = Object.freeze({
  low: 'low',
  medium: 'medium',
  high: 'high',
});
